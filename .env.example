# .env.example — copier vers .env.local et renseigner

# Proxy serverless PDH (Option A). En prod Vercel : /api/pdh (par défaut).
REACT_APP_PDH_PROXY=/api/pdh

# Token Mapbox GL (sera utilisé pour les scènes carte — actes 2 & 3)
REACT_APP_MAPBOX_TOKEN=


s4Z/H+f7r6VRdw4c
